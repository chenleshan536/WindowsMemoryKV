cmake_minimum_required(VERSION 3.10)

# Set the project name and version
project(WindowsMemoryKV VERSION 1.0)

# Set C++ standard
set(CMAKE_CXX_STANDARD 11)
set(CMAKE_CXX_STANDARD_REQUIRED True)

# Add the subdirectories
add_subdirectory(src/MemoryKVLib)
add_subdirectory(src/CppClientSample)
add_subdirectory(src/WindowsMemoryKVService)

# Optionally, if you want to include CSharp projects, 
# you would use MSBuild or another build system to handle those.
# However, for this `CMakeLists.txt`, we will focus on C++ projects.

# Include directories for the root project (if needed)
include_directories(${CMAKE_SOURCE_DIR}/src/MemoryKVLib)

# Define the installation rules (optional, depending on needs)
# For example, to install the binaries after building:
# install(TARGETS CppClientSample DESTINATION bin)
